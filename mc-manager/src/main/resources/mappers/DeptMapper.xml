<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.mdxq.wwjclub.ums.dao.DeptMapper">
    <insert id="insert">
       insert into ums_dept (title, info, fk_room_id)
       values (#{title},#{info},#{fkRoomId})
    </insert>

    <delete id="delete">
        update ums_dept
        set deleted = 1, updated = current_timestamp
        where id = #{param1}
    </delete>

    <delete id="deleteBatch">
        update ums_dept
        set deleted = 1, updated = current_timestamp
        <where>
            id IN <foreach collection="list" open="(" close=")" separator=",">#{id}</foreach>
        </where>
    </delete>

    <update id="update">
        update ums_dept
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="info != null">info = #{info},</if>
            <if test="fkRoomId != null">fk_room_id = #{fkRoomId},</if>
            version = version + 1
            updated = current_timestamp
        </set>
        where id = #{id} and version = #{version} and deleted = 0
    </update>

    <!--只查询未删除（deleted = 0）的记录-->
    <select id="selectById" resultType="dept">
        select * from ums_dept where id = #{param1} and deleted = 0
    </select>

    <resultMap id="RM_Dept" type="dept" autoMapping="true">
        <id column="id" property="id"/>
        <result column="fk_room_id" property="fkRoomId"/>
        <association column="fk_room_id" property="room"
                     select="org.mdxq.wwjclub.rms.dao.RoomMapper.selectById"/>
    </resultMap>

    <select id="list" resultMap="RM_Dept">
        select * from ums_dept
        <where>
            <if test="title != null"> title like '%${title}%' and </if>
            deleted = 0
        </where>
    </select>

    <select id="listAll" resultType="org.mdxq.wwjclub.ums.vo.DeptVO">
        select id, title from ums_dept where deleted = 0
    </select>

    <select id="countByTitle" resultType="java.lang.Long">
        select count(*) from ums_dept where title=#{param1} and deleted = 0
    </select>

</mapper>
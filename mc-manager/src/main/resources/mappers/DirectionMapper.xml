<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.mdxq.wwjclub.cms.dao.DirectionMapper">
    <insert id="insert">
        insert into cms_direction(title,info)
        values (#{title},#{info})
    </insert>

    <update id="update">
        update cms_direction
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="info != null">info = #{info},</if>
            <!--            <if test="updated != null">updated = #{updated},</if>-->
            version = version + 1,
            updated = current_timestamp
        </set>
        where id = #{id} and version = #{version} and deleted = 0
    </update>

    <delete id="delete">
        update cms_direction
        set deleted = 1,updated = current_timestamp
        where id = #{param1}
    </delete>

    <delete id="deleteBatch">
        update cms_direction
        set deleted = 1,updated = current_timestamp
        <where>
            id IN <foreach collection="list" item="id" open="(" close=")" separator=",">#{id}</foreach>
        </where>
    </delete>
    <!--只查询未删除（deleted = 0）的记录-->
    <select id="selectById" resultType="direction">
        select * from cms_direction where id = #{param1} and deleted = 0
    </select>

    <select id="list" resultType="direction">
        select * from cms_direction
        <where>
            <if test="title != null and title != ''">
                title like '%${title}%' and
            </if>
            deleted = 0
        </where>
    </select>

    <select id="listAll" resultType="org.mdxq.wwjclub.cms.vo.DirectionVO">
        select id,title from cms_direction where deleted = 0
    </select>
</mapper>